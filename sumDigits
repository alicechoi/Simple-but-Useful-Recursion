//adds digits of a number
public int sumDigits(int n) {
  if (n < 10) {
    return n;
  }
  else {
  //mod returns rightmost digit
    return n%10 + sumDigits(n/10); //divide by 10 removes rightmost digit
  }
}
